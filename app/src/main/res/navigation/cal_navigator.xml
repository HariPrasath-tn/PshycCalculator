<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cal_navigator"
    app:startDestination="@id/portrait">
    <fragment
        android:id="@+id/portrait"
        android:name="com.example.pshyccalculator.view.Portrait"
        android:label="Portrait" >
        <action
            android:id="@+id/action_portrait_to_calHistory"
            app:destination="@id/calHistory" />
    </fragment>
    <fragment
        android:id="@+id/calHistory"
        android:name="com.example.pshyccalculator.view.CalHistory"
        android:label="CalHistory" />
</navigation>